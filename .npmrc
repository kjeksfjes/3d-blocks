# Pin this project to the public npm registry.
# Without this, the global ~/.npmrc points npm at a private work feed, which
# bakes internal registry URLs into package-lock.json and breaks `npm ci` in CI.
registry=https://registry.npmjs.org/
